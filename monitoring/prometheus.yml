global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    project: aqi-platform

scrape_configs:
  - job_name: aqi_backend
    static_configs:
      - targets: ["aqi_backend:8000"]
    metrics_path: /metrics
    scrape_interval: 15s
